<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{title}}</title>
        <link rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
            integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2"
            crossorigin="anonymous">
    </head>
    <body>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th class="h3 text-{{titleCssClassName}}" scope="col"
                        colspan="6">
                        {{title}}
                    </th>
                </tr>
                <tr>
                    <th class="h5" scope="col">名次</th>
                    <th class="h5" scope="col">代號</th>
                    <th class="h5" scope="col">價格</th>
                    <th class="h5" scope="col">成交量</th>
                    <th class="h5" scope="col">漲跌幅</th>
                    <th class="h5" scope="col">漲跌</th>
                </tr>
            </thead>
            <tbody>



                {{#each items as |item index|}}
                <tr>
                    <th class="h5" scope="row">{{item.rank}}</th>
                    <td>
                        <p class="h4">{{item.id}}</p>
                        <p class="h5">{{item.name}}</p>

                    </td>
                    <td class="h4">{{item.price}}</td>
                    <td class="h4">{{item.dealCountText}}</td>
                    <td class="h4 text-{{../titleCssClassName}}">
                        {{item.riseRateText}}
                    </td>
                    <td class="h4 text-{{../titleCssClassName}}">
                        {{item.rise}}
                    </td>

                </tr>
                {{/each}}


            </tbody>
        </table>

    </body>
</html>